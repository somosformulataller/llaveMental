'use client';

import RankingPanel from '@/components/ranking/RankingPanel';

// Página /ranking: el mismo panel que abre el modal del header.
export default function RankingPage() {
  return (
    <main>
      <RankingPanel />
    </main>
  );
}
